services:
  pitcrew-backend:
    build:
      context: .
      dockerfile: Dockerfile
    image: pitcrew-backend:latest
    container_name: pitcrew-backend
    restart: unless-stopped
    env_file:
      - .env
    environment:
      - DATABASE_URL=sqlite:////app/data/app.db
    volumes:
      - pitcrew_data:/app/data
    # Direct port exposure for pre-Traefik / debug access; remove or
    # bind to 127.0.0.1 once Traefik is fronting this service.
    ports:
      - "0.0.0.0:8000:8000"
    networks:
      - proxy
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://localhost:8000/health').read()"]
      interval: 30s
      timeout: 5s
      retries: 3
      start_period: 20s
    labels:
      - "traefik.enable=true"
      - "traefik.docker.network=proxy"
      - "traefik.http.routers.pitcrew-backend.rule=Host(`pitcrew-backend.pxndey.com`)"
      - "traefik.http.routers.pitcrew-backend.entrypoints=web"
      - "traefik.http.services.pitcrew-backend.loadbalancer.server.port=8000"

networks:
  proxy:
    external: true

volumes:
  pitcrew_data:
